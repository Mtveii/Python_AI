Підготовка: віртуальне оточення
> cd ..                                у VSCode - переходимо "над" проєктом
> python -m venv Python_Ai             PythonAiP33 - назва проєкту
> cd Python_Ai                         повертаємось у проєкт 
> ./Scripts/Activate.ps1               активуємо оточення
бажано зробити .gitignore, вилучити з  репозиторію дані про оточення
створюємо директорію для робочих файлів

Інструментарій:
https://jupyter.org/
(!! в активованому віртуальному оточенні)
> pip install -U jupyter
 > pip install -U pandas
  > pip install -U numpy  
   > pip install -U matplotlib
    > pip install -U statsmodels
     > pip install -U scikit-learn
      > pip install -U torch
       > pip install -U torchvision
        > pip  insatll -U nltk
> pip freeze > libs.txt 

> pip install -r libs.txt |& jupyter notebook
> jupyter notebook


python -m pip install numpy  
python -m pip install pandas
python -m pip install jupyter
python -m pip install matplotlib
python -m notebook


---------------------------
Ubuntu 

> cd ..                                у VSCode - переходимо "над" проєктом
> python3 -m venv Python_AI            PythonAiP33 - назва проєкту
> cd Python_Ai                         повертаємось у проєкт 
> ./bin/activate                       активуємо оточення